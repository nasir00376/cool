import React from 'react';

const SideBar = () => {
    return (
        <nav id="sidebar">
            <div class="sidebar-header">
                <h3>Dashboard</h3>
            </div>
        </nav>
    );
};
export default SideBar;